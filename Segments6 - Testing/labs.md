### Change the directory to the unit-test-example: 
`cd Segments6/unit-test-example`

### RUN: 
`go mod init exampleTesting`

### RUN the command below to pull the dependencies
`go mod tidy`

### RUN:
`go test`